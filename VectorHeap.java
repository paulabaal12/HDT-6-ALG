import java.util.PriorityQueue;
import java.util.Scanner;

public class VectorHeap {

{
    PriorityQueue<Integer> vectorHeap = new PriorityQueue<>();
    vectorHeap.add(10);
    vectorHeap.add(5);
    vectorHeap.add(15);

    System.out.println("El elemento con mayor prioridad es: " + vectorHeap.peek());


}
}